[project]
name = "nexa"
version = "0.1.0"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
[tool.pytest.ini_options]
markers = [
    "slow: tests that load embedding models (deselect with -m 'not slow')",
]
